name: Deploy to Server

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: self-hosted  # <--- MUDANÇA IMPORTANTE: Roda no seu PC
    steps:
      - name: Executar Script de Deploy
        run: ~/deploy.sh  # Roda direto, sem SSH
